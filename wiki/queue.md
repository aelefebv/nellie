---
created: 2026-05-06
modified: 2026-05-09
---


# Queue — open architectural questions

Unresolved decisions, design questions, or known unknowns surfaced by the wiki seeding pass. Add items as they arise; remove them when resolved (with a pointer to the decision article or commit that resolved them).

## Open

- **macOS hard-pinned to CPU** in `nellie/__init__.py` — the MPS branch is commented out. Will Apple Silicon GPU support be revived? See [[gpu-runtime]].
- **`SettingsConfig` round-trip in the [[settings|napari settings widget]] is dead code today** — preset save/load not wired. Ship or remove?
- **Hu-moment cost weighting is unjustified in code** — z-scored sum of distance + stats + Hu blocks with no learned weights or rationale committed. Document the heuristic or replace? See [[hu-tracking]].
- **`flow_interpolation.py __main__` block has a `self.im_info` typo bug** — uses `self` outside a class. Remove or fix. See [[flow-interpolation]].
- **Plugin-menu injection touches napari private API** (`viewer.window._qt_window` + literal `"&Plugins"` string). Will silently break on napari ≥ 0.5 menu changes. See [[napari-plugin/index|napari plugin]] and [[loader]].
- **`Hierarchy.skip_nodes` defaults differ between callers.** [[pipeline|`run.py`]] hard-codes `False`; the [[settings|napari settings widget]] exposes it as a flag. Reconcile?
- **OOM detection in `adaptive_run` string-sniffs `"out of memory"`.** Fragile across CuPy/CUDA driver versions — new error wordings silently break the fallback. See [[gpu-runtime]].
- **Verifier (`im_info/verifier.py`) is unpinned by tests.** The legacy `test_verifier_metadata.py` was wiped during the May 2026 scaffold rebuild and not restored. The dechaos report at [[outputs/dechaos-verifier|`wiki/outputs/dechaos-verifier.md`]] (Pass 7) lists the full contract surface: per-format `dim_res` parsing (OME, ImageJ, ImageJ-fallback to TIFF tags, ND2 axesCalibration + median-of-diffs T, raw TIFF tag unit conversion incl. all 3 RESUNIT cases), `_normalize_time_axis` leading-singleton heuristic, the half-commented `change_axes` gate (decide what to pin via Q1 in the dechaos open questions), `_validate`'s asymmetric raise (Q2), `_normalize_axes` vs `_axis_errors` allowed-set divergence on `C` (Q3), `change_dim_res` / `change_selected_channel` / `select_temporal_range` validation contracts, `save_ome_tiff` channel-collapse + T-slicing, OME provenance JSON round-trip, `ImInfo._normalize_axes` / `_normalize_memmap` Z-squeeze, `ImInfo` auto-regen-on-construction behavior, `pipeline_paths` 18-key surface. ~50–80 characterization tests; new `tests/test_verifier.py`. Per-format fixtures need to be generated (or extend `tests/fixtures/_generate.py`). See [[im-info]] and the dechaos report's Slice 1 in Pass 8. **This slice unblocks Slice 2+ (clarify, constructor-I/O extraction, unified normalizer, validation split, extractor strategy).**
- **Cross-stage Config validation pass** — add lightweight `__post_init__` checks to all 7 Configs ([[filtering|`FrangiConfig`]], `LabelConfig`, `NetworkConfig`, `MarkersConfig`, `HuMomentTrackingConfig`, `VoxelReassignerConfig`, `HierarchyConfig`) for the obvious cases (string-set membership for `device` / `mode`, non-negative numerics, ordered `min_radius_um <= max_radius_um` ranges). Eligible immediately. Each Config's validation is independent; could land as one PR or per-stage micro-PRs. Pinned by existing characterization tests (no behavior change at default values). Deferred from PRD #112 per resolved decision #4.
- **Napari widget Config-return typing pass** — switch all 7 `get_*_params` methods in `nellie_napari/nellie_settings.py` to return Config objects directly (including [[filtering|`Filter`]] retrofit). The processor's `_run_*` methods become one-liners. Imports each stage's Config class into the napari widget module. Eligible immediately. Deferred from PRD #112 per resolved decision #5.

## Deferred wiki work

- **INTERVIEW pass not yet run.** DERIVE + LINT done on 2026-05-06. INTERVIEW is the next step in the INIT bootstrap chain — captures tribal knowledge that code can't reveal: why decisions were made, footguns from past incidents, the *why* behind designs DERIVE could only guess at (e.g., the unjustified Hu-moment cost weighting; the rationale for skipping nodes by default; the napari plugin's choice not to call `run.py`). Run `/repo-wiki interview` when ready.

## Recently resolved

- **Verifier dechaos pass landed** (2026-05-09) — full 8-pass scan of `nellie/im_info/verifier.py` (`FileInfo` ~680 lines + `ImInfo` ~370 lines). Output at [[outputs/dechaos-verifier|`wiki/outputs/dechaos-verifier.md`]] (commit `d663e50`). Confirmed all 5 suspect findings from the queue brief: 3 near-duplicate axes-normalization implementations (`_normalize_time_axis` / `_normalize_axes` / `_normalize_memmap`) with subtly different Z-squeeze + T-prepend logic; validation split across 7 methods with asymmetric-raise contract (`_validate` raises only on time errors, silent on axis/dim); half-commented `change_axes` gate where `self.good_axes = False` runs unconditionally before being overwritten by `_check_axes` (net no-op dead state); hardcoded `__main__` path; 5-way `metadata_type` string dispatch into per-format `_get_*_metadata` methods that take differently-shaped `metadata` args (no shared interface). Surfaced two **new** findings the brief didn't anticipate: (a) `_normalize_axes` excludes `C` from allowed axes while `_axis_errors` includes it — divergent contracts (Q3); (b) `_get_tif_tags_metadata` reads `self.axes` mid-extraction (implicit ordering dependency on `find_metadata` having run first). Sequenced 8 refactor slices: Slice 1 (tests, paired predecessor) → Slice 2 (clarify) → Slice 3 (constructor I/O) → Slices 4–7 (unified normalizer / validation split / extractor strategy / OME-TIFF writer) → Slice 8 (`load_image()` orchestrator). 5 open questions captured at the end of the report for the test-pinning slice to resolve before freezing behavior.

- **Cross-stage `*Config` dataclass extraction landed** (2026-05-09) — closes PRD #112 (issues #113, #114, #115). Slice 1 (PR #116) added `LabelConfig` / `NetworkConfig` / `MarkersConfig`. Slice 2 (PR #117) added `HuMomentTrackingConfig` (with the lifted `cost_cutoff` field — `_COST_CUTOFF` module constant gone) and `VoxelReassignerConfig`. Slice 3 (this PR) added `HierarchyConfig` and closes the queue item. Every algorithmic stage now has a frozen Config colocated above its class, mirroring the [[filtering|`FrangiConfig`]] pattern from PRD #66. Two follow-ups queued (validation pass; napari widget Config-return typing pass).
- **Hierarchy three-slice rollout landed** (2026-05-08) — closes PRD #105 (issues #106, #107, #108). Slice 1 (PR #109) added `tests/test_hierarchical.py` (~35 characterization tests pinning output schema, aggregation parity, motility short-circuits, branch length / thickness / tortuosity, reassigned-label fallback, backend characterization, per-call OOM fallback, outer cascade) and the heaviest conftest cascade extension to date (Filter→Label→Network→Markers→Hu→VoxelReassigner session caches + 9–11-file `_make_hierarchical_imageinfo_factory`). Slice 2 (PR #110) deleted dead helpers (`_get_t`, `create_feature_array`, the `__main__` block, 8 dead `self.X = None` init lines), dropped the `use_gpu` constructor parameter (Option A, mirroring Markers PR #89; napari widget + `SettingsConfig` + UI checkbox followed). Slice 3 (this PR) deleted `_cupy_available`, `_resolve_device`, and the module-level `try: import cupy as cp / _HAS_CUPY = True` block; constructor + `_set_backend` call `adaptive_run.resolve_backend` directly; `Branches._compute_branch_lengths_and_degrees` now gates on `self.hierarchy.device_type == "cuda"`, calls `self.hierarchy.xp` for the GPU module, catches the broader OOM family via `adaptive_run.is_oom_error` (was: narrow `cp.cuda.memory.OutOfMemoryError`), explicitly calls `adaptive_run.free_gpu_memory(self.hierarchy.xp)` on OOM, and propagates non-OOM exceptions instead of silently swallowing them — matches Hu PR #97 / VoxelReassigner PR #104. Hierarchy was the last stage in the per-stage test+hoist queue (joining Markers ✓, Hu ✓, VoxelReassigner ✓); the cross-stage `*Config` dataclass slice is now eligible. See [[feature-extraction]].
- **VoxelReassigner backend hoist + Option A2 inner-cascade refactor landed** (2026-05-08) — Slice 3 of PRD #98 deleted `_resolve_backend`, `_try_import_cupy`, `_is_oom_error`, `_free_gpu_memory`, and `_switch_to_cpu` from `VoxelReassigner`. Constructor and `_set_backend` now call `adaptive_run.resolve_backend` directly; the only stage-local backend code left is `_get_gpu_kdtree_cls()` (irreducible — `adaptive_run.try_import_cupy` returns `(cupy, ndimage)`, not the spatial KDTree). All 4 inner OOM cascade sites (`_build_tree` GPU KDTree OOM, `_query_tree` GPU branch, `_query_tree` GPU bruteforce branch, plus dead non-OOM `else` branch) reshaped to local-only CPU rebuild — no cross-frame `self.device_type` mutation; subsequent frames retry GPU. Latent silent-fallback bug at Sites 3+4 fixed via explicit `if not adaptive_run.is_oom_error(exc): raise`. Warning rate-limit moved to `_warn_gpu_fallback(reason)`. Guarded by Slice 1's 27 characterization tests (3 architecture-characterization tests inverted to pin the new contract). See [[voxel-reassignment]].
- **Network backend hoist landed** (2026-05-08) — Slice 3 of PRD #77 deleted `_resolve_backend`, `_try_import_cupy`, `_is_oom_error`, `_free_gpu_memory`, and `_switch_to_cpu` from `Network`; constructor and `_set_backend` now call `adaptive_run.resolve_backend` directly, inner OOM handlers call `adaptive_run.is_oom_error` / `adaptive_run.free_gpu_memory(self.xp)`, and `_run_frame`'s GPU-OOM handler uses `self._set_backend("cpu")` directly. No precursor needed — `adaptive_run.is_oom_error` already covered Network's `"OutOfMemory"` substring (lowercased to `"outofmemory"`) thanks to PRD #70's PR3 widening. Guarded by Slice 1's 17 characterization tests. See [[networking]] and PR #80.
- **Network dead-code consolidation landed** (2026-05-08) — Slice 2 of PRD #77 deleted `_clean_junctions`, `_local_max_peak`, the LoG/sigma machinery (`_set_default_sigmas`, `_get_sigma_vec`, `self.sigmas`), the `__main__` block, dead instance state (`self.network_memmap`, `self.shape = ()`, `self.debug`), the vestigial `_get_t`, and the unreachable `force_cpu=False` branch of `_remove_connected_label_pixels` (and its `force_cpu` parameter); also normalized the `device="cuda"` alias through `adaptive_run.normalize_device`. Guarded by Slice 1's 17 characterization tests. See [[networking]] and PR #82.
- **`nellie/run copy.py` deleted** (2026-05-07) — confirmed orphan, no importers. Removed alongside `cli.py` cleanup.
- **`nellie/cli.py` deleted** (2026-05-07) — broken since `run.py`'s signature change; no console-script entry point referenced it; not worth fixing for a stale UI nobody uses.
