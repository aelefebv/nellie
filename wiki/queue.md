---
created: 2026-05-06
modified: 2026-05-09
---

# Queue — open architectural questions

Unresolved decisions, design questions, or known unknowns surfaced by the wiki seeding pass. Add items as they arise; remove them when resolved (with a pointer to the decision article or commit that resolved them).

## Open

- **macOS hard-pinned to CPU** in `nellie/__init__.py` — the MPS branch is commented out. Will Apple Silicon GPU support be revived? See [[gpu-runtime]].
- **`SettingsConfig` round-trip in the [[settings|napari settings widget]] is dead code today** — preset save/load not wired. Ship or remove?
- **Hu-moment cost weighting is unjustified in code** — z-scored sum of distance + stats + Hu blocks with no learned weights or rationale committed. Document the heuristic or replace? See [[hu-tracking]].
- **`flow_interpolation.py __main__` block has a `self.im_info` typo bug** — uses `self` outside a class. Remove or fix. See [[flow-interpolation]].
- **Plugin-menu injection touches napari private API** (`viewer.window._qt_window` + literal `"&Plugins"` string). Will silently break on napari ≥ 0.5 menu changes. See [[napari-plugin/index|napari plugin]] and [[loader]].
- **`Hierarchy.skip_nodes` defaults differ between callers.** [[pipeline|`run.py`]] hard-codes `False`; the [[settings|napari settings widget]] exposes it as a flag. Reconcile?
- **OOM detection in `adaptive_run` string-sniffs `"out of memory"`.** Fragile across CuPy/CUDA driver versions — new error wordings silently break the fallback. See [[gpu-runtime]].
- **Wiki refresh for the verifier dechaos arc.** The [[im-info]] article was last meaningfully updated in Slice 1 (2026-05-09 morning). It still describes some pre-refactor structure: `_get_*_metadata` methods, the polymorphic `metadata` field, `_check_*` helpers, asymmetric `_validate` raise. Refresh to reflect: `load_image(path)` as canonical entry point, `nellie/im_info/extractors/` subpackage as the per-format strategy seam, `_write_ome_tiff` helper, symmetric (no-raise) `_validate`, `from_file_info` classmethod, deletion of `metadata`/`_check_axes`/`_check_dim_res`. Eligible immediately. Single small wiki edit.
- **ND2 characterization tests deferred** (sub-task of the verifier test-pinning slice above). The `nd2` library is read-only — no programmatic ND2 generation. No ND2 sample currently checked in. Two paths: (a) source a tiny single-frame `.nd2` and check it into `tests/fixtures/` (then add fixture-driven tests for `_get_nd2_metadata` covering `volume.axesCalibration` + `channels[0].volume.axesCalibration` fallback + `Time [s]` median-of-diffs + single-timepoint `T=None`); (b) skip ND2 tests entirely until the bioio integration (queued separately) renders the per-format ND2 path obsolete. **Eligible** when an ND2 sample becomes available OR after bioio integration lands.
- **Cross-stage Config validation pass** — add lightweight `__post_init__` checks to all 7 Configs ([[filtering|`FrangiConfig`]], `LabelConfig`, `NetworkConfig`, `MarkersConfig`, `HuMomentTrackingConfig`, `VoxelReassignerConfig`, `HierarchyConfig`) for the obvious cases (string-set membership for `device` / `mode`, non-negative numerics, ordered `min_radius_um <= max_radius_um` ranges). Eligible immediately. Each Config's validation is independent; could land as one PR or per-stage micro-PRs. Pinned by existing characterization tests (no behavior change at default values). Deferred from PRD #112 per resolved decision #4.
- **Napari widget Config-return typing pass** — switch all 7 `get_*_params` methods in `nellie_napari/nellie_settings.py` to return Config objects directly (including [[filtering|`Filter`]] retrofit). The processor's `_run_*` methods become one-liners. Imports each stage's Config class into the napari widget module. Eligible immediately. Deferred from PRD #112 per resolved decision #5.
- **Broaden file-format support via `bioio`.** Today the verifier (`nellie/im_info/verifier.py`) hard-dispatches on extension into `_find_tif_metadata` / `_find_nd2_metadata` (5 `metadata_type` branches: `'ome'`, `'imagej'`, `'imagej_tif_tags'`, `'nd2'`, `None`). Adding new formats (CZI, LIF, LSM, etc.) currently means hand-writing a new branch + a new `_get_*_metadata` extractor. `bioio` (https://github.com/bioio-devs/bioio) and its plugin ecosystem (`bioio-czi`, `bioio-lif`, `bioio-bioformats`, etc.) wrap most microscopy formats behind a single `BioImage(path)` API with a normalized `physical_pixel_sizes` + axes/shape/dtype interface. Migration path: introduce a `BioioExtractor` after Slice 6's `MetadataExtractor` strategy seam lands (per [[outputs/dechaos-verifier|verifier dechaos]] Pass 8), which subsumes most existing extractors and adds CZI/LIF/etc. for free. Trade-off: adds `bioio` (+ format plugins) to dependencies; some formats need optional installs (`bioio-bioformats` requires Java); `bioio`'s `physical_pixel_sizes` is in micrometers (matches our `dim_res`) but axes/dim ordering may need translation. **Do not start before** the verifier dechaos refactor's Slice 6 (extractor strategy) lands — `bioio` integration is much cleaner once the strategy seam exists.

## Deferred wiki work

- **INTERVIEW pass not yet run.** DERIVE + LINT done on 2026-05-06. INTERVIEW is the next step in the INIT bootstrap chain — captures tribal knowledge that code can't reveal: why decisions were made, footguns from past incidents, the *why* behind designs DERIVE could only guess at (e.g., the unjustified Hu-moment cost weighting; the rationale for skipping nodes by default; the napari plugin's choice not to call `run.py`). Run `/repo-wiki interview` when ready.

## Recently resolved

- **🎉 Verifier dechaos refactor — full 8-slice arc COMPLETE** (2026-05-09) — closes PRDs #119/#121/#123/#125/#127/#129/#131/#133 (PRs #120/#122/#124/#126/#128/#130/#132/#134) per the dechaos report at [[outputs/dechaos-verifier|`wiki/outputs/dechaos-verifier.md`]]. Slice 1: characterization tests (122). Slice 2: clarify pass + restored `change_axes` length gate. Slice 3: constructor I/O extraction (`from_file_info`). Slice 4: unified axes normalizer (`infer_t_axis` + `transform_to_axes`). Slice 5: validation split (`compute_errors` + `apply_defaults`, no more asymmetric raise). Slice 6: extractor strategy + bioio prep (5 per-format extractors in `nellie/im_info/extractors/` subpackage). Slice 7: OME-TIFF writer extraction (`_write_ome_tiff`). Slice 8: `load_image()` orchestrator. **Net pyright impact: 93 → 38 errors on verifier.py (−55 net, 59% reduction)**. Test suite grew from 147 baseline → 324 (+177 tests, all of `test_verifier_fileinfo.py` + `test_verifier_iminfo.py` + `test_extractors.py`). Single big behavior change across the arc: the polymorphic `fi.metadata` field deleted (Slice 6); other behavior changes (gate restoration, asymmetric-raise removal, `from_file_info` classmethod) are intentional contract improvements pinned by tests. Bioio integration is now unblocked.

- **Verifier OME-TIFF writer extraction landed** (2026-05-09) — closes PRD #131 (PR #132). Smallest structural slice. Extracted `_write_ome_tiff(path, *, data, shape, dtype, axes, dim_res, description)` helper in `verifier.py` shared by `FileInfo.save_ome_tiff` and `ImInfo.allocate_memory`. ~73 lines of duplicated write+re-open-OME-XML logic deleted from the two callers, replaced with a single 60-line helper. Subtle behavior change: `save_ome_tiff` no longer sets `physical_size_z = None` unconditionally for 2D fixtures (now left at tifffile default — provenance JSON unchanged, no test impact). 5 new tests pinning the helper directly. Pyright on verifier.py: 48 → 38 errors (−10 net). Test suite: 321 passed (316 baseline + 5 new). Slice 7 of the verifier dechaos refactor.

- **Verifier extractor strategy landed** (2026-05-09) — closes PRD #129 (PR #130, squashed into `dechao` as commit `9336345`). The structural payoff slice. New `nellie/im_info/extractors/` subpackage with 7 files: `MetadataExtractor` Protocol + 5 per-format dataclass implementations (`OmeExtractor`, `ImageJExtractor`, `ImageJTifTagExtractor`, `Nd2Extractor`, `RawTiffTagExtractor`) + `detect_extractor(filepath)` factory. `nellie/im_info/types.py` (new) holds `DimRes` + `infer_t_axis` to break the circular import. `verifier.py`: −200 net lines, 6 methods deleted (`_find_*_metadata` × 2, `_get_*_metadata` × 4), polymorphic `self.metadata` field deleted (verified zero in-tree readers). `find_metadata` is now 5 lines; `load_metadata` is 3. `ImageJTifTagExtractor` carries `imagej_meta` + `tif_tags` as proper named fields — no more list-of-dicts shoehorn (the smoking gun the dechaos report Pass 2 #2 flagged). 29 new tests in `tests/test_extractors.py` (pure-logic for per-extractor + 8 factory dispatch). All 287 existing tests pass without modification. Pyright on verifier.py: 83 → 48 errors (−35 net) thanks to polymorphic field deletion + 6 method removals. Test suite: 316 passed (287 baseline + 29 new). **Unblocks the queued bioio integration** — once the strategy seam exists, `BioioExtractor` is a 1-file addition.

- **Verifier validation split landed** (2026-05-09) — closes PRD #127 (PR #128, squashed into `dechao` as commit on top of Slice 4). Untangled the 7 overlapping validation methods into a clean structure: deleted `_check_axes` + `_check_dim_res` (dual-write wrappers), renamed `get_validation_errors` → `compute_errors`, added `apply_defaults()` (extracted t_start/t_end mutator), rewrote `_validate` as a thin orchestrator with **no raise** (Slice 1's asymmetric-raise pin flipped). `select_temporal_range` still raises directly on invalid input — `_validate`'s raise was a redundant second guardrail. Napari fileselect already checks validation_errors, so user-facing UX unchanged. 4 new tests for `apply_defaults`, 2 tests renamed for `compute_errors`, 1 napari callsite updated. Pyright on verifier.py: 85 → 83 errors (−2 net). Test suite: 287 passed (283 baseline + 4 new). Slice 5 of the verifier dechaos refactor.

- **Verifier unified axes normalizer landed** (2026-05-09) — closes PRD #125 (PR #126, squashed into `dechao` as commit `82ef16e`). Consolidated 3 near-duplicate axes-normalization implementations (`FileInfo._normalize_time_axis`, `ImInfo._normalize_axes`, `ImInfo._normalize_memmap`) into 2 module-level pure functions: `infer_t_axis(source_axes, source_shape)` (axes-string heuristic) and `transform_to_axes(data, source_axes, target_axes=None)` (data-shape transformation with canonical-vs-match modes). 4 call sites updated, 3 methods deleted. 17 existing direct-method tests renamed/migrated, 7 new pure-logic tests added (including the previously un-exercised "Z in target but not source" path). One error-message wording change: "Z axis present with size > 1, but ImInfo expects no Z axis" → "Z axis present with size > 1, but target axes lacks Z" (the unified function isn't ImInfo-aware). Pyright on verifier.py: 86 → 85 errors (−1 net). Test suite: 283 passed (276 baseline + 7 new). Slice 4 of the verifier dechaos refactor.

- **Verifier constructor I/O extraction landed** (2026-05-09) — closes PRD #123 (PR #124, squashed into `dechao` as commit `949dcc6`). First structural slice in the verifier dechaos arc. `FileInfo.__init__` and `ImInfo.__init__` no longer touch the filesystem. New: `FileInfo.prepare_output_dirs()` (auto-called from `find_metadata`), `ImInfo.load()` (idempotent), `ImInfo.from_file_info()` classmethod (canonical entry point). 12 callsite updates across production + tests (all `ImInfo(fi)` → `ImInfo.from_file_info(fi)`). 7 new tests pinning constructor purity, `load()` idempotency, `from_file_info` equivalence, and `find_metadata`'s auto-call wiring. Pyright: 86 errors in verifier.py (unchanged from Slice 2 baseline). Test suite: 276 passed (269 baseline + 7 new). **Unlocks pure-logic testing** for Slices 4-8 (subsequent slices can test in-memory transformations without filesystem fixtures).

- **Verifier clarify pass landed** (2026-05-09) — closes PRD #121 (PR #122, squashed into `dechao` as commit `753bc3d`). Six clarify items + one intentional behavior change. Behavior change: restored the `change_axes` length gate (originally added in commit `bb2b0b7`, disabled by `492edfb` for napari fileselect partial-typing UX — but napari pre-validates, so the gate was redundant for napari and missing for programmatic callers). On length mismatch now raises `ValueError`; `self.axes` is preserved on failure. Slice 1 test `test_change_axes_bad_length_mutates_axes_and_flags_invalid` flipped to `test_change_axes_bad_length_raises`. Other items: deleted `__main__` block (hardcoded path); added `DimRes` TypedDict for `dim_res`; reset `no_z`/`no_t` symmetrically in `ImInfo._check_axes_exist`; made `_get_tif_tags_metadata`'s `axes` an explicit parameter; added Preconditions sections to all 4 mutator docstrings. Pyright in verifier.py: 93 → 86 errors (−7 net). Test suite: 269 passed. Slice 2 of the verifier dechaos refactor — unblocks Slice 3 (constructor I/O extraction).

- **Verifier characterization tests landed** (2026-05-09) — closes PRD #119 (PR #120, squashed into `dechao` as commit `4996475`). 122 tests across `tests/test_verifier_fileinfo.py` (76 tests) + `tests/test_verifier_iminfo.py` (46 tests), runtime ~3s. 5 new ~2KB fixtures generated by `tests/fixtures/_generate.py` covering 4 of 5 `metadata_type` branches (OME, ImageJ-with-physicalsize, ImageJ-without-physicalsize, raw-TIFF × 3 RESUNIT cases — ND2 deferred). Conftest extended with 5 path constants + `copy_fixture_to_tmp` + `verifier_fixture_paths`. 3 design quirks pinned with explicit comments (FileInfo accepts C / ImInfo rejects C; `_validate` asymmetric raise; half-commented `change_axes` gate). 3 production behaviors discovered during testing flagged in [[im-info]]: tifffile strips singleton axes on readback (worth flagging for Slice 7), `change_dim_res`'s None-check fires before invalid-dim check, `select_temporal_range` raises distinct messages for negative start vs end. Slice 1 of the verifier dechaos refactor — unblocks Slices 2–8.

- **Verifier dechaos pass landed** (2026-05-09) — full 8-pass scan of `nellie/im_info/verifier.py` (`FileInfo` ~680 lines + `ImInfo` ~370 lines). Output at [[outputs/dechaos-verifier|`wiki/outputs/dechaos-verifier.md`]] (commit `d663e50`). Confirmed all 5 suspect findings from the queue brief: 3 near-duplicate axes-normalization implementations (`_normalize_time_axis` / `_normalize_axes` / `_normalize_memmap`) with subtly different Z-squeeze + T-prepend logic; validation split across 7 methods with asymmetric-raise contract (`_validate` raises only on time errors, silent on axis/dim); half-commented `change_axes` gate where `self.good_axes = False` runs unconditionally before being overwritten by `_check_axes` (net no-op dead state); hardcoded `__main__` path; 5-way `metadata_type` string dispatch into per-format `_get_*_metadata` methods that take differently-shaped `metadata` args (no shared interface). Surfaced two **new** findings the brief didn't anticipate: (a) `_normalize_axes` excludes `C` from allowed axes while `_axis_errors` includes it — divergent contracts (Q3); (b) `_get_tif_tags_metadata` reads `self.axes` mid-extraction (implicit ordering dependency on `find_metadata` having run first). Sequenced 8 refactor slices: Slice 1 (tests, paired predecessor) → Slice 2 (clarify) → Slice 3 (constructor I/O) → Slices 4–7 (unified normalizer / validation split / extractor strategy / OME-TIFF writer) → Slice 8 (`load_image()` orchestrator). 5 open questions captured at the end of the report for the test-pinning slice to resolve before freezing behavior.

- **Cross-stage `*Config` dataclass extraction landed** (2026-05-09) — closes PRD #112 (issues #113, #114, #115). Slice 1 (PR #116) added `LabelConfig` / `NetworkConfig` / `MarkersConfig`. Slice 2 (PR #117) added `HuMomentTrackingConfig` (with the lifted `cost_cutoff` field — `_COST_CUTOFF` module constant gone) and `VoxelReassignerConfig`. Slice 3 (this PR) added `HierarchyConfig` and closes the queue item. Every algorithmic stage now has a frozen Config colocated above its class, mirroring the [[filtering|`FrangiConfig`]] pattern from PRD #66. Two follow-ups queued (validation pass; napari widget Config-return typing pass).
- **Hierarchy three-slice rollout landed** (2026-05-08) — closes PRD #105 (issues #106, #107, #108). Slice 1 (PR #109) added `tests/test_hierarchical.py` (~35 characterization tests pinning output schema, aggregation parity, motility short-circuits, branch length / thickness / tortuosity, reassigned-label fallback, backend characterization, per-call OOM fallback, outer cascade) and the heaviest conftest cascade extension to date (Filter→Label→Network→Markers→Hu→VoxelReassigner session caches + 9–11-file `_make_hierarchical_imageinfo_factory`). Slice 2 (PR #110) deleted dead helpers (`_get_t`, `create_feature_array`, the `__main__` block, 8 dead `self.X = None` init lines), dropped the `use_gpu` constructor parameter (Option A, mirroring Markers PR #89; napari widget + `SettingsConfig` + UI checkbox followed). Slice 3 (this PR) deleted `_cupy_available`, `_resolve_device`, and the module-level `try: import cupy as cp / _HAS_CUPY = True` block; constructor + `_set_backend` call `adaptive_run.resolve_backend` directly; `Branches._compute_branch_lengths_and_degrees` now gates on `self.hierarchy.device_type == "cuda"`, calls `self.hierarchy.xp` for the GPU module, catches the broader OOM family via `adaptive_run.is_oom_error` (was: narrow `cp.cuda.memory.OutOfMemoryError`), explicitly calls `adaptive_run.free_gpu_memory(self.hierarchy.xp)` on OOM, and propagates non-OOM exceptions instead of silently swallowing them — matches Hu PR #97 / VoxelReassigner PR #104. Hierarchy was the last stage in the per-stage test+hoist queue (joining Markers ✓, Hu ✓, VoxelReassigner ✓); the cross-stage `*Config` dataclass slice is now eligible. See [[feature-extraction]].
- **VoxelReassigner backend hoist + Option A2 inner-cascade refactor landed** (2026-05-08) — Slice 3 of PRD #98 deleted `_resolve_backend`, `_try_import_cupy`, `_is_oom_error`, `_free_gpu_memory`, and `_switch_to_cpu` from `VoxelReassigner`. Constructor and `_set_backend` now call `adaptive_run.resolve_backend` directly; the only stage-local backend code left is `_get_gpu_kdtree_cls()` (irreducible — `adaptive_run.try_import_cupy` returns `(cupy, ndimage)`, not the spatial KDTree). All 4 inner OOM cascade sites (`_build_tree` GPU KDTree OOM, `_query_tree` GPU branch, `_query_tree` GPU bruteforce branch, plus dead non-OOM `else` branch) reshaped to local-only CPU rebuild — no cross-frame `self.device_type` mutation; subsequent frames retry GPU. Latent silent-fallback bug at Sites 3+4 fixed via explicit `if not adaptive_run.is_oom_error(exc): raise`. Warning rate-limit moved to `_warn_gpu_fallback(reason)`. Guarded by Slice 1's 27 characterization tests (3 architecture-characterization tests inverted to pin the new contract). See [[voxel-reassignment]].
- **Network backend hoist landed** (2026-05-08) — Slice 3 of PRD #77 deleted `_resolve_backend`, `_try_import_cupy`, `_is_oom_error`, `_free_gpu_memory`, and `_switch_to_cpu` from `Network`; constructor and `_set_backend` now call `adaptive_run.resolve_backend` directly, inner OOM handlers call `adaptive_run.is_oom_error` / `adaptive_run.free_gpu_memory(self.xp)`, and `_run_frame`'s GPU-OOM handler uses `self._set_backend("cpu")` directly. No precursor needed — `adaptive_run.is_oom_error` already covered Network's `"OutOfMemory"` substring (lowercased to `"outofmemory"`) thanks to PRD #70's PR3 widening. Guarded by Slice 1's 17 characterization tests. See [[networking]] and PR #80.
- **Network dead-code consolidation landed** (2026-05-08) — Slice 2 of PRD #77 deleted `_clean_junctions`, `_local_max_peak`, the LoG/sigma machinery (`_set_default_sigmas`, `_get_sigma_vec`, `self.sigmas`), the `__main__` block, dead instance state (`self.network_memmap`, `self.shape = ()`, `self.debug`), the vestigial `_get_t`, and the unreachable `force_cpu=False` branch of `_remove_connected_label_pixels` (and its `force_cpu` parameter); also normalized the `device="cuda"` alias through `adaptive_run.normalize_device`. Guarded by Slice 1's 17 characterization tests. See [[networking]] and PR #82.
- **`nellie/run copy.py` deleted** (2026-05-07) — confirmed orphan, no importers. Removed alongside `cli.py` cleanup.
- **`nellie/cli.py` deleted** (2026-05-07) — broken since `run.py`'s signature change; no console-script entry point referenced it; not worth fixing for a stale UI nobody uses.
